.venv
.venv/
.codex

# Local crash dumps and scheduler outputs
core.*
slurm-*.out
slurm-*.err
logs/
timing_results/
# run_feasibility.sh force-adds its own result directory, so ignoring this by
# default keeps stray local runs out of commits without blocking the ones we want.
results/

# Python and editor noise
__pycache__/
*.pyc
.pytest_cache/

# Large local artifacts and cloned references
repos/

# Keep Overleaf sync untouched by excluding the nested git repo entirely
paper/

# Local agent config
.claude/

# Review material kept locally, not part of the released code
paper.pdf
rebuttal.pdf
