\section{Results}
\label{sec:results}

\subsection{Main Results}
\label{sec:main}

We test \name on three MoE LLMs, OLMoE-1B-7B, Qwen1.5-MoE-A2.7B and Mixtral-8x7B, against six risky inputs from PandaGuard JBB and two helpfulness benchmarks XSTest and MMLU, comparing to the no-intervention \textit{Original} and the static \textit{SteerMoE}~\cite{fayyaz2025steermoe} with both methods intervening on $K=25$ experts. Table~\ref{tab:main} illustrates that \name improves safety substantially while preserving helpfulness, whereas \textit{SteerMoE}'s safety gains come at a $12$\,pp average XSTest cost. The two-sided improvement traces back to the two design choices of Section~\ref{sec:method}. On the safety side, per-query identification targets the experts that actually amplify risk on the current input, while \textit{SteerMoE}'s fixed pool inherits aggregate statistics that miss the per-query culprits. On the helpfulness side, per-query modulation collapses the bias to zero on benign inputs where $\lambda(q)$ falls below the calibrated threshold, while static \textit{SteerMoE} applies the same suppression to benign and risky queries alike.

\input{tables/1_main_results.tex}

The gain is also broad across the five JBB harmful-behavior categories, with \emph{Malware/Hacking} and \emph{Economic harm} showing the largest absolute lifts. These two categories also exhibit the strongest $\drefuse$ separation at the probe layer. Figure~\ref{fig:ablations-overview}~(a) measures, within each category, how strongly $\lambda(q) = h(q) \cdot \drefuse$ at $\ell^*$ separates risky prompts from benign ones; \emph{Malware/Hacking} and \emph{Economic harm} show the strongest separation, which makes the risk gauge $\lambda(q)$ most reliable precisely where the largest lifts emerge.

\begin{figure}[!h]
\centering
\includegraphics[width=\linewidth]{figures/fig_ablations_overview.pdf}
\caption{(a) AUROC of $\lambda(q) = h(q) \cdot \drefuse$ in classifying benign vs risky prompts within each of the five JBB harmful-behavior categories (H/D = Harassment/Discrimination, M/H = Malware/Hacking, PH = Physical harm, EH = Economic harm, F/D = Fraud/Deception). (b) Pairwise-Jaccard distributions across 200 risky inputs ($K{=}25$ over $1024$ experts): dark = intra-\name, light = \name vs SteerMoE static pool, dotted gray = chance. (c) Gradient rank ($1$ = most negative $\Delta$) versus safe-rate increase after ablating that single expert. Spearman $\rho = -0.81$ over $1{,}250$ pairs ($50$ queries $\times$ top-$25$); negative because rank $1$ corresponds to the largest increase.}
\label{fig:ablations-overview}
\end{figure}

\subsection{Ablation Studies}
\label{sec:ablations}
\noindent\textbf{Identification is per-query and causal.}
We test three sub-properties: (i) the identified set varies across queries (it is not a fixed pool returned regardless of input); (ii) the set is not equivalent to any global safety-relevant pool, ruling out the possibility that the per-query selection is a noisy reading of a single static pool; and (iii) the gradient ranking is faithful to the actual causal effect of suppressing each expert.
Figure~\ref{fig:ablations-overview}~(b) addresses (i) and (ii) through two pairwise-Jaccard distributions across 200 risky inputs. The intra-\name distribution has mean $0.12$, far below $1.0$, so the set varies substantially across queries (i). The \name-vs-SteerMoE distribution has mean $0.03$, close to chance, so the per-query sets do not even overlap with SteerMoE's static pool (ii). Figure~\ref{fig:ablations-overview}~(c) addresses (iii): ablating each top-$K$ expert in turn across $1{,}250$ (query, expert) pairs gives Spearman $\rho = -0.81$ between gradient rank and post-ablation safe-rate increase, confirming that the gradient ranking is faithful to the actual causal effect on safety.

\begin{wraptable}{r}{0.45\linewidth}
\vspace{-1em}
\input{tables/2_ablation.tex}
\vspace{-1em}
\end{wraptable}

\noindent\textbf{On the importance of modulation.}
Identification picks the right experts but cannot say how strongly to suppress them. We treat \emph{modulation} as the joint magnitude design in Equation~\ref{eq:bias} comprising both factors that scale the bias: the query-level ramp $\rho(\lambda(q))$ that adapts to query risk and the per-expert weight $w_{\ell,e}(q)$ that adapts to attribution strength. A flat $-\beta$ replaces both with constants, applying the same suppression on every query and on every identified expert.
We test this by setting $\rho \equiv 1$ and $w \equiv 1$ in Equation~\ref{eq:bias}, reducing \name to a flat $-\beta$ on each identified expert. Table~\ref{tab:modulation} shows that XSTest drops $7$\,pp, leaving \name close to SteerMoE on helpfulness despite per-query identification. Safety also drops $3.3$\,pp on average and MMLU $1$\,pp. Identification and modulation are complementary by design, and neither alone is sufficient.

\begin{wraptable}{r}{0.45\linewidth}
\vspace{-1em}
\input{tables/3_ood_generalization.tex}
\vspace{-1em}
\end{wraptable}

\noindent\textbf{Generalization to unseen risky prompts.}
We test whether \name's offline directions $\drefuse$, $\dbeh$ and the calibration of $(\lambda_{\text{lo}}, \lambda_{\text{hi}})$ generalize beyond the risky-prompt distributions used to extract and calibrate them. We restrict every offline step (direction extraction and ramp calibration) to $\{\text{GCG}, \text{PAIR}\}$ and evaluate on the same six-attack test set as the main table, so the four held-out attacks $\{\text{PastTense}, \text{AutoDAN}, \text{ICA}, \text{Renellm}\}$ are unseen at every offline step. Table~\ref{tab:lambda-ood} reports an average safety of $42.5$ over the six-attack test set, about $4$\,pp below \name's in-distribution average ($46.5$ in Table~\ref{tab:main}, where offline saw all six attacks) and still well above SteerMoE's $31.7$ on the same six-attack set. The drop is small relative to the gain over SteerMoE, suggesting the offline directions and ramp endpoints encode a generic risky-versus-benign distinction rather than features specific to particular risky-prompt patterns.

\noindent\textbf{Probe-layer selection.}\label{sec:probe-ablation}
The probe layer $\ell^*$ feeds both the identification gradient and the modulation signal $\lambda(q)$, so its choice can affect either or both mechanisms. On a held-out calibration set of $200$ risky and benign prompts, disjoint from the test prompts in the main results, we sweep $\ell^* \in \{4, 7, 10, 13, 15\}$ on OLMoE-1B-7B and measure identification quality via downstream safe rate and helpfulness via XSTest answer rate.

Identification benefits from a deeper probe: safe rate rises from $52\%$ at $\ell^* = 4$ to $62\%$ at $\ell^* = 10$ and saturates across $\ell^* \in \{10, 13\}$, consistent with a deeper probe attributing over more layers until the top-$K{=}25$ budget saturates. Modulation is sharply peaked in the middle: XSTest reaches $98$ at $\ell^* = 10$ and drops at both ends ($85$ at $\ell^* = 4$, $88$ at $\ell^* = 15$). Early layers have not yet encoded an abstract risk distinction, while the final layer specializes to generation rather than carrying it.

Since identification has saturated across mid-layers, we select the mid-layer with the highest XSTest, giving $\ell^* = 10$ on OLMoE. Applying the same criterion to Qwen1.5-MoE-A2.7B-Chat and Mixtral-8x7B-Instruct-v0.1 yields layers at comparable depth fractions ($0.58$ and $0.625$ respectively, vs $0.625$ for OLMoE; full per-model values in Appendix~\ref{app:model-details}), consistent with a mid-layer plateau that does not depend on the specific MoE backbone.

\begin{wraptable}{r}{0.45\linewidth}
\vspace{-1em}
\input{tables/4_negresult.tex}
\vspace{-1em}
\end{wraptable}

\noindent\textbf{Suppression vs amplification.}
A natural symmetric variant of \name promotes the top-$K$ \emph{positive}-$\Delta$ experts (denoted $\Apos(q)$) instead of suppressing $\Amin(q)$, applying a positive router bias of equal magnitude. Table~\ref{tab:negresult} shows this collapses across all metrics. Average safety drops to $0\%$, XSTest answer rate also collapses to $0\%$, and generation perplexity under Llama-3.1-8B reaches $>10^{3}$ on the amplification variant, compared to $12.0$ on \textit{Original} and $12.3$ on \name's suppression variant. The asymmetry comes from MoE's top-$k$ routing structure. Suppression with $\beta=-10$ pushes $K{=}25$ experts out of the top-$k$ active set, leaving the remaining $E-K$ experts to fill the top-$k$ slots through normal input-conditioned competition. Amplification with $\beta=+10$ instead concentrates almost all routing softmax mass on the same $K{=}25$ experts on every token, so the active top-$k$ subset is drawn from this fixed pool of $25$; with OLMoE's top-$k = 8$, the router still has $\binom{25}{8}$ combinatorial slack within the pool, but the other $E-K$ experts are starved of input-conditioned selection. This restricts the input-conditioned competition from $E = 64$ experts down to $K = 25$ on every token, collapsing the per-input specialization that gives MoE its capability, and generation degenerates rather than steering toward refusal. $\Apos(q)$ is therefore not a coherent ``safe-expert pool'' that can be amplified into; it is a set of experts whose marginal contribution to $s$ is positive at the current routing, and forcing them into the active set destroys the very routing mechanism that the gradient was computed against.